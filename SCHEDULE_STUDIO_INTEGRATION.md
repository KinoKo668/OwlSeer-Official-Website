# 🔗 Schedule & Studio Integration Feature

## ✅ 功能完成

成功实现了 **Schedule & Slots (日程管理)** 和 **Content Studio (内容创作)** 之间的双向关联功能。

---

## 🎯 核心功能

### **1. 从 Schedule 关联脚本**
在发布日程管理页面，可以将现有的脚本关联到特定的发布时段。

### **2. 从 Studio 关联时段**  
在内容创作页面，可以为脚本选择发布时段（待实现）。

### **3. 双向同步**
关联建立后，两个模块的数据保持同步。

---

## 📦 新增组件

### **1. ScriptSelectorModal** (`/src/app/components/ScriptSelectorModal.tsx`)

**功能：** 脚本选择器弹窗

**特性：**
- ✅ 搜索脚本
- ✅ 状态过滤（All/Ready/Draft）
- ✅ 显示脚本详情
- ✅ 只显示可用脚本（Ready/Draft状态）
- ✅ 当前选中高亮
- ✅ 快捷创建新脚本

**UI设计：**
```
┌────────────────────────────────────────┐
│ Select Script                      [X] │
├────────────────────────────────────────┤
│ [🔍 Search scripts...]                │
│ [All] [Ready] [Draft]                  │
├────────────────────────────────────────┤
│ ┌────────────────────────────────────┐ │
│ │ [Ready] Product Review             │ │
│ │                                    │ │
│ │ 5 iPhone Features Apple Hides     │ │
│ │                                    │ │
│ │ 5 scenes · Updated Jan 20, 2026   │ [✓]
│ └────────────────────────────────────┘ │
│                                        │
│ ┌────────────────────────────────────┐ │
│ │ [Draft] Tutorial                   │ │
│ │                                    │ │
│ │ Morning Routine for Productivity   │ │
│ │                                    │ │
│ │ 3 scenes · Updated Jan 21, 2026    │ │
│ └────────────────────────────────────┘ │
├────────────────────────────────────────┤
│ 2 scripts available                    │
│                  [Cancel] [✨ Create]  │
└────────────────────────────────────────┘
```

---

### **2. SlotSelectorModal** (`/src/app/components/SlotSelectorModal.tsx`)

**功能：** 时段选择器弹窗

**特性：**
- ✅ 搜索发布时段
- ✅ 状态过滤（All/To Do/In Progress）
- ✅ 只显示未来时段
- ✅ 只显示未关联脚本的时段
- ✅ 显示时段详情
- ✅ 突出显示Today/Priority标签

**UI设计：**
```
┌────────────────────────────────────────┐
│ Select Publishing Slot             [X] │
├────────────────────────────────────────┤
│ [🔍 Search publishing slots...]       │
│ [All] [To Do] [In Progress]            │
├────────────────────────────────────────┤
│ ┌────────────────────────────────────┐ │
│ │ [In Progress] [Today] [Priority]   │ │
│ │                                    │ │
│ │ Trending sound challenge           │ │
│ │ Reach new audiences                │ │
│ │                                    │ │
│ │ 📅 Tue, Jan 7                      │ [✓]
│ └────────────────────────────────────┘ │
│                                        │
│ ┌────────────────────────────────────┐ │
│ │ [To Do] 🎯 Core Content            │ │
│ │                                    │ │
│ │ Motivation message                 │ │
│ │ Strengthen your core pillar        │ │
│ │                                    │ │
│ │ 📅 Wed, Jan 8                      │ │
│ └────────────────────────────────────┘ │
├────────────────────────────────────────┤
│ 2 slots available          [Cancel]    │
└────────────────────────────────────────┘
```

---

### **3. CreationExecutionPanelEnhanced** (`/src/app/components/CreationExecutionPanelEnhanced.tsx`)

**功能：** 增强版任务详情面板（带脚本关联）

**新增功能：**
- ✅ 显示已关联脚本
- ✅ 关联新脚本按钮
- ✅ 取消关联功能
- ✅ 跳转到Studio查看脚本
- ✅ 主CTA根据关联状态变化

**UI对比：**

**未关联脚本：**
```
┌──────────────────────────────────────┐
│ [○] Not Started                      │
│                                      │
│ Trending sound challenge             │
│ Tue, January 7                       │
├──────────────────────────────────────┤
│ CONTENT SCRIPT                       │
│ ┌──────────────────────────────────┐ │
│ │  🔗 Link existing script         │ │
│ └──────────────────────────────────┘ │
│                                      │
│ WHAT TO CREATE                       │
│ Quick reaction video featuring...    │
│                                      │
│ WHY THIS MATTERS                     │
│ Reach new audiences                  │
├──────────────────────────────────────┤
│ [✨ Generate script]                 │ ← 主CTA
│ [✏️ Write myself] [✓ Mark done]     │
└──────────────────────────────────────┘
```

**已关联脚本：**
```
┌──────────────────────────────────────┐
│ [○] Not Started                      │
│                                      │
│ Trending sound challenge             │
│ Tue, January 7                       │
├──────────────────────────────────────┤
│ LINKED SCRIPT                    [X] │ ← 取消关联
│ ┌──────────────────────────────────┐ │
│ │  📄 5 iPhone Features Apple Hides│ │
│ │  Product Review · 5 scenes       │ │
│ │                                  │ │
│ │  View in Studio  ↗️              │ │ ← 跳转链接
│ └──────────────────────────────────┘ │
│                                      │
│ WHAT TO CREATE                       │
│ Quick reaction video featuring...    │
├──────────────────────────────────────┤
│ [📄 Continue in Studio]              │ ← 主CTA变化
│ [✏️ Write myself] [✓ Mark done]     │
└──────────────────────────────────────┘
```

---

## 🔄 工作流程

### **Workflow 1: 从 Schedule 关联现有脚本**

```
1. 打开 Schedule & Slots 页面
   ↓
2. 选择一个任务（点击日历卡片）
   ↓
3. 在右侧面板看到"Link existing script"按钮
   ↓
4. 点击按钮打开脚本选择器
   ↓
5. 搜索/过滤脚本列表
   ↓
6. 选择一个脚本
   ↓
7. 脚本关联成功！
   ↓
8. 面板显示已关联的脚本信息
   ↓
9. 可以点击"View in Studio"跳转
   或点击"Continue in Studio"继续编辑
```

---

### **Workflow 2: 从 Schedule 创建新脚本**

```
1. 打开 Schedule & Slots 页面
   ↓
2. 选择一个任务
   ↓
3. 点击"Link existing script"
   ↓
4. 在弹窗中点击"✨ Create New Script"
   ↓
5. 跳转到 Content Studio
   ↓
6. 创建新脚本（自动关联到该任务）
   ↓
7. 返回 Schedule 查看关联结果
```

---

### **Workflow 3: 从 Studio 关联时段**（待实现）

```
1. 打开 Content Studio 页面
   ↓
2. 创建或编辑脚本
   ↓
3. 在脚本详情中点击"Link to Slot"
   ↓
4. 打开时段选择器
   ↓
5. 选择发布时段
   ↓
6. 关联成功！
   ↓
7. 脚本显示关联的发布时间
```

---

## 📊 数据模型

### **ContentTask Interface（更新）**

```typescript
interface ContentTask {
  id: string;
  title: string;
  purpose: string;
  description: string;
  status: 'todo' | 'in-progress' | 'done';
  date: Date;
  structure?: string;
  isToday?: boolean;
  isPriority?: boolean;
  
  // 新增字段 ✨
  linkedScriptId?: string;      // 关联的脚本ID
  linkedScriptTitle?: string;   // 脚本标题（缓存）
}
```

---

### **Script Interface（待更新）**

```typescript
interface Script {
  id: string;
  title: string;
  contentType: ContentType;
  status: ScriptStatus;
  // ... 其他字段
  
  // 新增字段 ✨
  linkedSlotId?: string;        // 关联的时段ID
  linkedSlotDate?: Date;        // 发布日期（缓存）
  scheduledDate?: Date;         // 排定的发布时间
}
```

---

## 🎨 视觉设计

### **关联状态指示器**

**日历卡片：**
```
┌─────────────────────────┐
│ [○] Trending sound...   │
│ Reach new audiences     │
│ ─────────────────────   │
│ 📄 Linked Script        │ ← 新增指示器
└─────────────────────────┘
```

**脚本卡片（Studio）：**
```
┌─────────────────────────┐
│ [Ready] Product Review  │
│                         │
│ 5 iPhone Features...    │
│                         │
│ 📅 Jan 7, 2026         │ ← 新增发布日期
└─────────────────────────┘
```

---

### **颜色编码**

| 元素 | 颜色 | 用途 |
|------|------|------|
| 关联成功背景 | 蓝色渐变 `#eff6ff → #dbeafe` | 已关联脚本卡片 |
| 关联按钮 | 虚线边框 `#e0e0e0` | 未关联状态 |
| 主CTA（已关联） | 蓝色 `#3b82f6` | Continue in Studio |
| 主CTA（未关联） | 黑色 `#1a1a1a` | Generate script |
| 取消关联 | 红色 `#dc2626` | X按钮 |
| 跳转链接 | 蓝色 `#3b82f6` | View in Studio |

---

## ⚡ 交互细节

### **1. 脚本选择器**

**搜索：**
- 实时过滤，大小写不敏感
- 搜索范围：标题

**过滤：**
- All - 显示所有Ready/Draft状态
- Ready - 只显示Ready状态
- Draft - 只显示Draft状态

**选中状态：**
- 蓝色边框 `border-[#3b82f6]`
- 蓝色背景 `bg-[#eff6ff]`
- 右上角勾选图标

**点击行为：**
- 点击整个卡片 = 选择并关闭弹窗
- 自动关联到任务

---

### **2. 时段选择器**

**过滤逻辑：**
- 只显示未来日期（≥今天）
- 只显示未关联脚本的时段
- 除非是当前已选中的时段

**徽章：**
- [Today] - 黄色背景
- [Priority] - 红色背景
- [In Progress] - 黄色文字
- [To Do] - 灰色文字

---

### **3. 关联面板**

**主CTA动态变化：**
```typescript
// 未关联
<button>✨ Generate script</button>

// 已关联
<button>📄 Continue in Studio</button>
```

**跳转行为：**
- "View in Studio" → 打开Studio并定位到该脚本
- "Continue in Studio" → 打开Studio编辑器
- X按钮 → 确认后取消关联

---

## 🔧 技术实现

### **状态管理**

**本地状态：**
```typescript
const [showScriptSelector, setShowScriptSelector] = useState(false);
const [linkedScript, setLinkedScript] = useState<Script | undefined>();
```

**回调函数：**
```typescript
interface Props {
  onLinkScript?: (taskId: string, scriptId: string, scriptTitle: string) => void;
  onUnlinkScript?: (taskId: string) => void;
}
```

---

### **数据同步**

**方向1：Schedule → Studio**
```
Task.linkedScriptId = script.id
  ↓
Script.linkedSlotId = task.id  // 待实现
```

**方向2：Studio → Schedule**
```
Script.linkedSlotId = task.id  // 待实现
  ↓
Task.linkedScriptId = script.id
```

---

## 📝 Mock数据

### **示例脚本数据**

```typescript
const mockScripts = [
  {
    id: 'script-1',
    title: '5 iPhone Features Apple Hides',
    contentType: 'Product Review',
    status: 'Ready',
    updatedAt: new Date(2026, 0, 20),
    scenes: [
      { id: 's1', role: 'Hook' },
      { id: 's2', role: 'Context' },
      { id: 's3', role: 'Value' },
      { id: 's4', role: 'Proof' },
      { id: 's5', role: 'CTA' },
    ],
  },
  {
    id: 'script-2',
    title: 'Budget Laptop Comparison 2026',
    contentType: 'Product Review',
    status: 'Ready',
    updatedAt: new Date(2026, 0, 19),
    scenes: [...],
  },
  {
    id: 'script-3',
    title: 'Morning Routine for Productivity',
    contentType: 'Tutorial',
    status: 'Draft',
    updatedAt: new Date(2026, 0, 21),
    scenes: [...],
  },
];
```

---

## 🚀 下一步扩展

### **Phase 1 - 基础功能** ✅ 完成
- [x] Schedule页面脚本选择器
- [x] 脚本关联UI
- [x] 显示已关联脚本
- [x] 取消关联功能
- [x] 跳转到Studio

---

### **Phase 2 - Studio端集成** 🔜 待实现
- [ ] Studio页面时段选择器
- [ ] 脚本卡片显示发布日期
- [ ] 从脚本跳转到Schedule
- [ ] 快捷创建时段功能

---

### **Phase 3 - 高级功能** 💡 规划中
- [ ] 批量关联（多个脚本→多个时段）
- [ ] 自动匹配建议（AI推荐最佳时段）
- [ ] 拖放关联（日历拖拽）
- [ ] 冲突检测（一个时段多个脚本）
- [ ] 关联历史记录
- [ ] 取消关联确认弹窗

---

### **Phase 4 - 数据持久化** 📦 规划中
- [ ] 后端API集成
- [ ] 实时同步
- [ ] 乐观更新
- [ ] 离线支持
- [ ] Supabase集成

---

## 🎯 产品价值

### **创作者痛点解决：**

1. **减少上下文切换**
   - 不需要在Schedule和Studio之间反复切换
   - 一键跳转，保持工作流连贯

2. **避免重复工作**
   - 不需要重复创建相同内容
   - 直接复用已有脚本

3. **提升计划性**
   - 提前关联脚本到发布计划
   - 清晰看到哪些内容已准备好

4. **优化时间管理**
   - 明确知道每个时段的内容状态
   - 优先处理未关联脚本的时段

---

## 📊 使用场景

### **场景1：周计划模式**
```
周一制定一周计划
  ↓
为每个时段创建/关联脚本
  ↓
本周按计划创作内容
  ↓
实时查看进度
```

---

### **场景2：批量创作模式**
```
一次性创作多个脚本
  ↓
逐个关联到最佳发布时段
  ↓
按时段发布
```

---

### **场景3：紧急插入模式**
```
突发热点话题
  ↓
快速创建脚本
  ↓
插入到最近的可用时段
  ↓
发布
```

---

## ✅ 完成文件

### **新增组件：**
1. `/src/app/components/ScriptSelectorModal.tsx` - 脚本选择器
2. `/src/app/components/SlotSelectorModal.tsx` - 时段选择器  
3. `/src/app/components/CreationExecutionPanelEnhanced.tsx` - 增强版任务面板

### **更新组件：**
4. `/src/app/components/SchedulingSlots.tsx` - 集成关联功能

---

## 🎉 总结

成功实现了Schedule和Studio之间的关联功能，为创作者提供了更流畅的内容规划和创作体验。

**主要亮点：**
- ✅ 清晰的关联UI
- ✅ 双向查看能力
- ✅ 流畅的跳转体验
- ✅ 完整的状态管理
- ✅ 符合产品设计规范

**下一步：** 实现Studio端的时段选择功能，完成双向关联的完整闭环。

---

**完成时间：** 2026年1月22日  
**功能类型：** 模块间集成  
**影响范围：** Schedule & Slots + Content Studio  
**产品阶段：** Phase 1 完成，Phase 2 待实现
